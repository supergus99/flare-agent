# Copy to .dev.vars and fill in. .dev.vars is gitignored.
# For production, use: npx wrangler secret put STRIPE_WEBHOOK_SECRET
# (and same for STRIPE_SECRET_KEY; publishable key is only needed in frontend if you use Stripe.js client-side)

STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Optional: Checkout branding (when using a sub-account, so Flare shows instead of parent/STR logo)
# CHECKOUT_DISPLAY_NAME=Flare
# CHECKOUT_LOGO_URL=https://getflare.net/path/to/logo.png
