{
  "name": "flare",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "node scripts/smoke.js",
    "test": "node scripts/smoke.js"
  },
  "devDependencies": {
    "wrangler": "^4.65.0"
  }
}
